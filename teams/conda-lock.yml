# NOTE: This file is a work in progress.
# The information here reflected is provisional
# and must not be interpreted as the ground truth.
# yaml-language-server: $schema=./teams.schema.json
name: conda-lock
description: Lightweight lockfile for conda environments
charter: federated-project
details:
resources:
  teams:
    - conda/conda-lock
  repos:
    - conda/conda-lock
  other:
links:
  - https://github.com/conda/conda-lock/issues/319
members:
  ericdill:
  jezdez:
  marcelotrevisani:
  maresb:
  mariusvniekerk:
  ocefpaf:
emeritus:
